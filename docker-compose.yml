# Dokploy / Compose — do NOT bind host:80 (Traefik handles routing)
services:
  sfxlab:
    build:
      context: .
      dockerfile: Dockerfile
    # Internal only — map your Dokploy domain to this port (80)
    expose:
      - "80"
    restart: unless-stopped
